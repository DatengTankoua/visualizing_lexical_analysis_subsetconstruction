import type { NFA, DFA } from "../models/types";

export function convertNFAtoDFA(nfa: NFA): DFA {
  // TODO: Implementierung der Teilmengen-Konstruktion
  console.log("Converting NFA to DFA...");
  return {
    ...nfa,
    // temp
  };
}
